<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itesm.a01019108.concesionapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SlpashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ClientMainActivity"
            android:label="@string/title_activity_client_main" />
        <activity
            android:name=".CarDetailsActivity"
            android:label="@string/title_activity_car_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SellerMainActivity"
            android:label="@string/title_activity_vendor_main" />
        <activity
            android:name=".ManagerMainActivity"
            android:label="@string/title_activity_manager_main" />
        <activity
            android:name=".DistributorMainActivity"
            android:label="@string/title_activity_distributor_main" />
        <activity android:name=".CarSaleDetailsActivity"></activity>
    </application>

</manifest>